#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

npx lint-staged 

# Following is for monitoring only 

#TODOs / FIXMEs
npx leasot 'src/**/*.[jt]s?(x)' --exit-nicely

# # Test coverage
# npx jest --coverage